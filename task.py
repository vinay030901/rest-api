import os
import requests
from dotenv import load_dotenv

load_dotenv()



#def send_user_registration_email(email, username):
    # return send_simple_message(email, "Successfully registered",
    #                            f"Hi {username}! You have successfully registered to the Stores REST API")
